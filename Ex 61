#include <stdio.h>

int main (){
    int n, contador = 0;

    for (int i = 1; i<=10; i++) {
        printf ("digite um valor:");
        scanf ("%d", &n);
        contador = contador + n;
    }

    printf ("a média e: %d", contador/10);
}
