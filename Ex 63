#include <stdio.h>

int main (){
    int n, soma=0;

    for (int i=1; i<=10; i++){
        printf ("digite um valor: ");
        scanf ("%d", &n);
        soma = soma + n;
    }

    printf ("soma = %d", soma);
}
